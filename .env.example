## Local-only configuration (do not commit `.env`)
#
# Parquet root directory layout:
#   {PARQUET_DIR}/{SYMBOL}/{YYYY}.parquet
#
# Example:
# PARQUET_DIR="/Volumes/ssd/us_stock_data/1s_parquet"

PARQUET_DIR="/path/to/1s_parquet"
